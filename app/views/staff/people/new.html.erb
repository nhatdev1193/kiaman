<% # Set the title attribute for this page
   content_for :title do %>
    People
<% end %>

<% # Set the title of content
   content_for :content_title do %>
    People <small>Add new</small>
<% end %>

<% if params[:type] == 'normal' %>
    <%= render 'form' %>
<% elsif params[:type] == 'multi' %>
    <%= render 'multi_form' %>
<% end %>
